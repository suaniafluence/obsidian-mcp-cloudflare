{
  "name": "obsidian-mcp-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "1.6.0",
    "wrangler": "3.80.0"
  }
}
